generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum Tipo {
  DOCE
  SALGADA
  BEBIDA
}

model Receita {
    id         Int       @id @default(autoincrement())
    receita       String
    tipo       Tipo
    ingredientes String
    modoFazer   String
    img         String
    custoAproximado Float       
}